version: "3.8"

services:

  grunt:
    image: huli/grunt
    container_name: "grunt"
    restart: "no"
    working_dir: "/app/grunt"
    entrypoint: "/bin/sh -c 'npm install && grunt package'"
    volumes:
      - type: bind
        source: ./
        target: /app
      