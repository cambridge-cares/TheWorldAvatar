process.env.NODE_ENV = 'test';


const chai = require('chai');

const assert = require('assert');
describe('Emission Agent Endpoint', function() {
    describe('#indexOf()', function() {
        it('should return 4 string values', function() {
            assert.equal(, 4, '');
        });
    });
});