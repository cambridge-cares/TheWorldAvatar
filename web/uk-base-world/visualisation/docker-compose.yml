version: "3.8"

# All Services
services:

  # Example DTVF implementation
  uk-power-system-vis:
    image: ghcr.io/cambridge-cares/dtvf-base-image:latest
    container_name: "uk-power-system-vis"
    ports:
      - "80:80"
    volumes:
      - ../webspace:/var/www/html
