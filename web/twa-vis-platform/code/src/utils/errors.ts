/**
 * Custom error class used when illegal arguments are parsed
 * into a method/class.
 */
export class IllegalArgumentError extends Error {}

/**
 * Custom error class used when the code reaches an
 * illegal state.
 */
export class IllegalStateError extends Error {}
