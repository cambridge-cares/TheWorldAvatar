# This script serves as an experiment for testing branched BERT model

# from Training.Util import
from keras import Model, Input

input_A = Input(shape=(None, 32), name='input_a')
b

model_nlp_branch = Model(inputs=[])