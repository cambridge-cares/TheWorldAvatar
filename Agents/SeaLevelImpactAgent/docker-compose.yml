version: "3.8"

services:
  sealevelimpactagent:
    image: ghcr.io/cambridge-cares/sealevelimpactagent:1.2.0-dev-integrate-sg-sea-level-SNAPSHOT
    build: .
    ports:
      - "10110:8080"
    volumes:
      - ./inputs:/inputs

