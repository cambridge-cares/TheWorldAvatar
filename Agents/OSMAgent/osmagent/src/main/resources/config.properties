# database name
db.name=postgres
# schema name of OpenStreetMap data
osm.schema=osm
# table name (inclusive of schema) and geometry column of land use data, leave empty if no land use data
landuse.table=public.dlmsie02f
# geometry column of land use data, leave empty if no land use data
landuse.geometry=wkb_geometry
# csv file name for land use tags in landuse.table (inclusive of .csv)
landuse.csv=dlm_landuse.csv