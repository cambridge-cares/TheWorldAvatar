version: "3.8"

services:
  email-sending-agent:
    image: email-sending-agent:1.0.0
    build: .
    container_name: email-sending-agent
    pull_policy: if_not_present
    ports:
      - "1021:8080"