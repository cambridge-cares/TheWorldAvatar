version: '3.8'

services:
  example:
    image: expsetup_agent:1.0.0
    container_name: expsetup_agent
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 7010:5000
