version: "3.8"

services:
  pips-agent:
    image: pips-agent:1.0.0
    build: .
    container_name: pips-agent
    pull_policy: if_not_present
    volumes:
      - './config:/root/config'
    ports:
      - "1080:8080"