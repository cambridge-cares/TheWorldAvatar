services:
  networkanalysisagent:
    image: ghcr.io/cambridge-cares/networkanalysisagent:1.3.0-consolidation-SNAPSHOT
    build: .
    ports:
      - "10107:8080"
    volumes:
      - ./inputs:/inputs

