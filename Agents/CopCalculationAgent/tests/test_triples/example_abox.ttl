@prefix example:		<http://www.example.org/derivationagentpythonexample#> .
@prefix rdf:			<http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
@prefix xsd:            <http://www.w3.org/2001/XMLSchema#> .
@prefix :			    <http://www.example.org/derivationagentpythonexample/data/> .

###############################################
## Derivation Agent Python Example instances ##
###############################################

# in this example, both maxValue and minValue instances are pure inputs
# they need timestamp to be properly retrieved by the derivation agent
# however, the timestamp is not provided in the example triples
# instead, it will be added by the script when the derivation instance is created for new info

:maxValue
    rdf:type example:MaxValue;
    example:hasValue :max;
.

:max
    rdf:type example:Value;
    example:numVal "15.3"^^xsd:double;
.

:minValue
    rdf:type example:MinValue;
    example:hasValue :min;
.

:min
    rdf:type example:Value;
    example:numVal "3.2"^^xsd:double;
.

# difference instance is not needed for the derivation
# as it will be generated by the derivation agent when derivation instance is created for new info
