version: '3.8'

services:
  example:
    image: doe_agent:1.0.0
    container_name: doe_agent
    environment:
      LOG4J_FORMAT_MSG_NO_LOOKUPS: "true"
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 7000:5000
