version: "3.8"

services:
  travellingsalesmanagent:
    image: ghcr.io/cambridge-cares/travellingsalesmanagent:1.2.0-dependency-SNAPSHOT
    build: .
    ports:
      - "10106:8080"
    volumes:
      - ./inputs:/inputs

