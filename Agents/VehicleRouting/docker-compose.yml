version: "3.8"

services:
  vehicle-routing:
    image: vehicle-routing:1.0
    deploy:
      mode: replicated
      replicas: 1
      restart_policy:
        condition: none
    configs:
      - postgis
      - blazegraph
    secrets:
      - postgis_password
    networks:
      - stack

