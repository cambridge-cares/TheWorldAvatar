version: '3.8'
services:
  environmental-exposure-calculation-agent:
    image: ghcr.io/cambridge-cares/environmental-exposure-calculation-agent:1.0.0
    build:
      context: .
      target: debug
    ports:
      - "${DEBUG_PORT}:5678"
    volumes:
      - ./agent:/app/agent
