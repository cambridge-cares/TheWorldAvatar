#!/bin/bash
# Copy all the following commands once, paste them into the terminal together, and then press Enter
curl -X POST http://localhost:3838/fenland-trajectory-agent/instantiate \
     -H "Content-Type: application/json" 
