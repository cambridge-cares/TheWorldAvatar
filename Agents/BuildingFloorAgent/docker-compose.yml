version: "3.8"

services:
  buildingflooragent:
    image: ghcr.io/cambridge-cares/buildingflooragent:1.0.0-SNAPSHOT
    container_name: buildingflooragent
    build:
      context: .
      target: agent
    ports:
      - "10104:8080"
    volumes:
      - ./src/main/resources:/resources