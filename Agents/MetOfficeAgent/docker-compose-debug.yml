version: '3.8'
services:
  metofficeagent:
    environment:
      - FLASK_APP=metoffice.flaskapp.wsgi.py
      - FLASK_ENV=development
    ports:
      - "5678:5678" # "${DEBUG_PORT}:5005"
    volumes:
      - ./metoffice:/app/metoffice      
      - ./output:/app/output
