version: '3.8'
services:
  metoffice_agent:
    image: docker.cmclinnovations.com/metoffice_agent_debug:1.0.0-SNAPSHOT
    build:
      context: .
      target: debug
    ports:
      - "${DEBUG_PORT}:5678"
    volumes:
      - ./metoffice:/app/metoffice
