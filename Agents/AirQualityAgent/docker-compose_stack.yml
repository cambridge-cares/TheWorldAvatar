version: "3.8"

services:
  airquality_agent_production:
    container_name: airquality_agent_production
    image: docker.cmclinnovations.com/airquality_agent_production:1.0.0-SNAPSHOT
    build:
      context: .
      target: prod
    ports:
      - "5000:5000"
    volumes:
      # Link container output directory to local directory to access output files
      - ./output:/app/output

  blazegraph_airquality:
    container_name: "blazegraph_airquality"
    # potentially different image to be used in the future
    image: docker.cmclinnovations.com/blazegraph_for_tests:1.0.0
    restart: unless-stopped
    ports:
      - "8889:9999"
    volumes:
      - blazegraph_data:/blazegraph

  postgres_airquality:
    container_name: "postgres_airquality"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    # potentially different image to be used in the future
    image: postgres:13.3
    restart: unless-stopped
    ports:
      - "7432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  blazegraph_data:
    name: "blazegraph_airquality_data"
  postgres_data:
    name: "postgres_airquality_data"