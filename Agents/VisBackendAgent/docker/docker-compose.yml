version: "3.9"

services:
  agent:
    container_name: vis-backend-agent
    image: ghcr.io/cmcl-io/vis-backend-agent:0.0.1-SNAPSHOT
    build:
      context: ..
      target: agent
    ports:
      - "8080:8080"