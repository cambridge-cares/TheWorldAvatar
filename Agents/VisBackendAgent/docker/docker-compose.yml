version: "3.9"

services:
  agent:
    container_name: vis-backend-agent
    image: ghcr.io/cambridge-cares/vis-backend-agent:0.0.1-SNAPSHOT
    build:
      context: ..
      target: agent
    volumes:
    - ../resources:/usr/local/tomcat/resources
    ports:
      - "8080:8080"