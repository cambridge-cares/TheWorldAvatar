version: "3.9"

services:
  agent:
    container_name: vis-backend-agent-test
    image: ghcr.io/cambridge-cares/vis-backend-agent:test
    build:
      context: ..
      target: test
    ports:
      - "8080:8080"