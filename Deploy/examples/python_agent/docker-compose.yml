version: '3.8'

services:
  example:
    image: python_agent_eg:1.0.0-SNAPSHOT
    container_name: python_agent_eg
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 7000:5000
