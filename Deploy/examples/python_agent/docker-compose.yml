version: '3.8'

services:
  example:
    image: python_agent_eg:1.0.0
    container_name: python_agent_eg
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 55002:5000
