version: "3.8"

# Services
services:

  # Blazegraph
  blazegraph:
    ports:
      - "7889:8080"

  # RDF4J
  rdf4j:
    ports:
      - "7888:8080"


# Test network
networks:
  default:
    name: test-db
