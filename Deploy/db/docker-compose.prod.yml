version: "3.8"
services:
  blazegraph:
    container_name: dev-blazegraph
    ports:
      - "6889:8080"
  rdf4j:
    container_name: dev-rdf4j
    ports:
      - "6888:8080"

networks:
  default:
    name: prod-db
