upstream psdt_feature-info-agent {
  # https://www.nginx.com/blog/avoiding-top-10-nginx-configuration-mistakes/#upstream-groups
  zone upstreams 64K;
  server psdt_feature-info-agent:8080;
  keepalive 2;
}
