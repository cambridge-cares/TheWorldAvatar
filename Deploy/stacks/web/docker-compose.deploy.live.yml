version: "3.8"

services:

  # Land Use Crop Map
  land-use:
    ports:
      - 50082:80

 # Reverse Proxy
 reverse_proxy:
   ports:
     - 80:80
     - 443:443

  # Gas Grid Visualisation
  uk-gas-grid:
    ports:
      - 50084:80

  # Power System and SDG Visualisation
  uk-power-system:
    ports:
      - 50083:80