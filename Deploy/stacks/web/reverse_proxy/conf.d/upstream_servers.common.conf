# List of upstream servers that are used for all container modes
upstream local_chatbot {
    server jps-chatbot:5000;
}

upstream local_ldf {
    server jps-ldf:3000;
}

upstream local_power_system {
    server uk-power-system:3001;
}

upstream local_gas_grid {
    server uk-gas-grid:80;
}

upstream local_un_goals {
    server un-goals:80;
}

upstream local_land_use {
    server land-use:80;
}

upstream local_website_http {
    server website:80;
}

# Both the prod and dev redirects point to the dev-db instance of blazegraph_geo for now
# Remove this server (and uncomment the corresponding entries in upstream_servers.dev.conf, upstream_servers.prod.conf) when the blazegraph_geo container is running on both dev-db and prod-db
upstream db_blazegraph_geo {
    192.168.1.218:8989
}