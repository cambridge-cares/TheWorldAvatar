version: "3.8"

# Services
services:

  # Reverse Proxy
  reverse_proxy:
    # Map http to port 81
    ports:
      - 81:80
      
  # Power System and SDG Visualisation
  uk-power-system:
    build:
      target: development


# Development network
networks:
  default:
    name: dev-web
