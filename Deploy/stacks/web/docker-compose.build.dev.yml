version: "3.8"

services:

  # Power System and SDG Visualisation
  uk-power-system:
    build:
      target: development

  # Gas Grid Visualisation
  uk-gas-grid:
    build:
      target: development
      
  # Land Use Crop Map
  land-use:
    build:
      target: development