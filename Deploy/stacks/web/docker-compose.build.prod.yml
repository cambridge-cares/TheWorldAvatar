version: "3.8"

services:

  # Power System and SDG Visualisation
  uk-power-system:
    build:
      target: production

  # Gas Grid Visualisation
  uk-gas-grid:
    build:
      target: production
      
  # Land Use Crop Map
  land-use:
    build:
      target: production