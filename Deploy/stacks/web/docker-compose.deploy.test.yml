version: "3.8"

services:

 # Reverse Proxy
 reverse_proxy:
   ports:
     - 1080:80
     - 10443:443

  # Power System and SDG Visualisation
  uk-power-system:
    ports:
      - 11083:80

  # Gas Grid Visualisation
  uk-gas-grid:
    ports:
      - 11084:80

  # Land Use Crop Map
  land-use:
    ports:
      - 11082:80