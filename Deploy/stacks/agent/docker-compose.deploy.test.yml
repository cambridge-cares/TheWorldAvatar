version: "3.8"

services:

  # Email Agent
  email-agent:
    ports:
      - 59080:8080

  # Gas Grid Agent
  gas-grid-agent:
    ports:
      - 51081:80

  # JPS Chatbot
  jps-chatbot:
    ports:
      - 56000:5000

  # JPS LDF Server
  jps-ldf:
    ports:
      - 54000:3000

  # Kinetics Agent
  kinetics-agent:
    ports:
      - 59081:8080