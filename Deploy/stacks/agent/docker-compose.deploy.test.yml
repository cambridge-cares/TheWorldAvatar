version: "3.8"

services:

  # Email Agent
  email-agent:
    ports:
      - 49080:8080

  # Gas Grid Agent
  gas-grid-agent:
    ports:
      - 41081:80

  # JPS Chatbot
  jps-chatbot:
    ports:
      - 46000:5000

  # JPS LDF Server
  jps-ldf:
    ports:
      - 44000:3000

  # # Kinetics Agent
  # kinetics-agent:
  #   ports:
  #     - 49081:8080