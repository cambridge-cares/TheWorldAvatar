version: "3.8"

services:

  # Email Agent
  email-agent:
    ports:
      - 48080:8080

  # Gas Grid Agent
  gas-grid-agent:
    ports:
      - 40081:80

  # JPS Chatbot
  jps-chatbot:
    ports:
      - 45000:5000

  # JPS LDF Server
  jps-ldf:
    ports:
      - 43000:3000

  # Kinetics Agent
  kinetics-agent:
    ports:
      - 48081:8080