version: "3.8"

# All Services
services:

  # Pirmasens Digital Twin visualisation
  psdt-visualisation-cesium:
    image: ghcr.io/cambridge-cares/dtvf-base-image:latest
    container_name: "psdt-visualisation-cesium"
    ports:
      - "42070:80"
    volumes:
      - ../webspace:/var/www/html
 
