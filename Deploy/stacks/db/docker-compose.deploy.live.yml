version: "3.8"

services:

  # Blazegraph
  blazegraph:
    ports:
      - 48082:8080

  # Blazegraph geo
  blazegraph-geo:
    ports:
      - 48083:8080

  # Postgres
  postgres:
    ports:
      - 45432:5432

  # RDF4J
  rdf4j:
    ports:
      - 48084:8080