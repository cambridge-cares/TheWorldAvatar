version: "3.8"

services:

  # Blazegraph
  blazegraph:
    ports:
      - 49082:8080

  # Blazegraph geo
  blazegraph-geo:
    ports:
      - 49083:8080

  # Postgres
  postgres:
    ports:
      - 46432:5432

  # RDF4J
  rdf4j:
    ports:
      - 49084:8080