version: "3.8"

# Services
services:

  # Blazegraph
  blazegraph:
    ports:
      - "8889:8080"

  # RDF4J
  rdf4j:
    ports:
      - "8888:8080"


# Development network
networks:
  default:
    name: dev-db
