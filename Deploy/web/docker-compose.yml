version: "3.8"

# Services
services:

  # Knowledge Graph Website
  website:
    build: "./website"
    image: "${MODE}-website-${HASH}"
    container_name: "${MODE}-website-${HASH}"
    restart: unless-stopped