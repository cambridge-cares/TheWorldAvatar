version: "3.8"

# Services
services:

  # Knowledge Graph Website
  website:
    build: "./website"
    image: docker.cmclinnovations.com/website:v1.0
    container_name: "${MODE}-website-${HASH}"
    restart: unless-stopped
