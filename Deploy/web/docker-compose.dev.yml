version: "3.8"

# Services
services:

  # Have reverse proxy serve http over port 80 in dev mode
  reverse_proxy:
    ports:
      - 80:80

# Development network
networks:
  default:
    name: dev-web

