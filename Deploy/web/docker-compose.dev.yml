version: "3.8"

# Services
services:

  # Have reverse proxy serve http over port 81 in dev mode
  reverse_proxy:
    ports:
      - 81:80

# Development network
networks:
  default:
    name: dev-web

