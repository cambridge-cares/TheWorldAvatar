version: "3.8"

# Services
services:
    
  # Knowledge Graph Website (Port 81)
  website:
    build: 
      context: "./website"
    ports:
      - "81:80"
   
# Development network    
networks:
  default:
    name: dev-web