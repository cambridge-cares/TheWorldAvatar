version: "3.8"

services:
  python-service:
    image: docker.cmclinnovations.com/python-service:1.0.0-SNAPSHOT
    build:
      target: production
    deploy:
      mode: replicated
      replicas: 1
      restart_policy:
        condition: none
    ports:
      - 5000:5000
    networks:
      - stack