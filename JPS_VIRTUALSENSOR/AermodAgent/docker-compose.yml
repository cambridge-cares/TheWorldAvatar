version: "3.8"

services:
  aermod-agent:
    image: ghcr.io/cambridge-cares/aermod-agent:1.5
    deploy:
      mode: replicated
      replicas: 1
      restart_policy:
        condition: none
    configs:
      - postgis
      - blazegraph
      - geoserver
      - ontop
    secrets:
      - postgis_password
      - geoserver_password
    networks:
      - stack
